# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: cuenv-ci
on:
  push:
    branches:
    - main
    paths:
    - '**'
    - .github/workflows/cuenv-ci.yml
    - cue.mod/**
    - env.cue
    - schema/**
  pull_request:
    branches:
    - main
    paths:
    - '**'
    - .github/workflows/cuenv-ci.yml
    - cue.mod/**
    - env.cue
    - schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  id-token: write
jobs:
  check-lint:
    name: check.lint
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: check.lint
      run: cuenv task check.lint --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        RUSTC_WRAPPER: sccache
  check-security:
    name: check.security
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: check.security
      run: cuenv task check.security --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        RUSTC_WRAPPER: sccache
  check-test:
    name: check.test
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: check.test
      run: cuenv task check.test --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        RUSTC_WRAPPER: sccache
