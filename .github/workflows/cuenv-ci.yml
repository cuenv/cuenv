# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: cuenv-ci
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/cuenv-ci.yml
      - Cargo.lock
      - Cargo.toml
      - _tests/**
      - crates/**
      - cue.mod/**
      - deny.toml
      - env.cue
      - examples/**
      - features/**
      - schema/**
      - treefmt.toml
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/cuenv-ci.yml
      - Cargo.lock
      - Cargo.toml
      - _tests/**
      - crates/**
      - cue.mod/**
      - deny.toml
      - env.cue
      - examples/**
      - features/**
      - schema/**
      - treefmt.toml
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
  id-token: write
jobs:
  check:
    name: check
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup Cachix (cuenv)
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix-env -iA cachix -f https://cachix.org/api/v1/install && cachix use cuenv
        env:
          CACHIX_CACHE_NAME: cuenv
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9
        env:
          RUSTC_WRAPPER: sccache
      - name: Build cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix develop -c cargo build --release -p cuenv && { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && ./target/release/cuenv sync -A
      - name: check
        run: cuenv task check --skip-dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CACHIX_CACHE_NAME: cuenv
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          RUSTC_WRAPPER: sccache
