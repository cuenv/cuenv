# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: cuenv-release
on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      tag_name:
        description: Tag to release (e.g., v0.16.0)
        required: true
        type: string
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
  id-token: write
jobs:
  release-build-linux-x64:
    name: release.build (linux-x64)
    runs-on: blacksmith-8vcpu-ubuntu-2404
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.build (linux-x64)
        run: cuenv task release.build -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CUENV_ARCH: linux-x64
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-build-linux-x64
          path: result/bin/*
          if-no-files-found: ignore
  release-build-linux-arm64:
    name: release.build (linux-arm64)
    runs-on: blacksmith-8vcpu-ubuntu-2404-arm
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.build (linux-arm64)
        run: cuenv task release.build -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CUENV_ARCH: linux-arm64
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-build-linux-arm64
          path: result/bin/*
          if-no-files-found: ignore
  release-build-darwin-arm64:
    name: release.build (darwin-arm64)
    runs-on: macos-14
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.build (darwin-arm64)
        run: cuenv task release.build -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CUENV_ARCH: darwin-arm64
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-build-darwin-arm64
          path: result/bin/*
          if-no-files-found: ignore
  release-publish-github:
    name: release.publish.github
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
      - release-build-linux-x64
      - release-build-linux-arm64
      - release-build-darwin-arm64
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: Download release-build-linux-x64
        uses: actions/download-artifact@v4
        with:
          name: release-build-linux-x64
          path: dist/linux-x64
      - name: Download release-build-linux-arm64
        uses: actions/download-artifact@v4
        with:
          name: release-build-linux-arm64
          path: dist/linux-arm64
      - name: Download release-build-darwin-arm64
        uses: actions/download-artifact@v4
        with:
          name: release-build-darwin-arm64
          path: dist/darwin-arm64
      - name: release.publish.github
        run: cuenv task release.publish.github -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PATHS: dist/**/*
          TAG: ${{ github.ref_name }}
  release-publish-homebrew:
    name: release.publish.homebrew
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
      - release-publish-github
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.publish.homebrew
        run: cuenv task release.publish.homebrew -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  release-publish-crates:
    name: release.publish.crates
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
      - release-publish-homebrew
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.publish.crates
        run: cuenv task release.publish.crates -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  release-publish-cue:
    name: release.publish.cue
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
      - release-publish-crates
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: release.publish.cue
        run: cuenv task release.publish.cue -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  docs-deploy:
    name: docs.deploy
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
      - release-publish-cue
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v16
        with:
          extra-conf: accept-flake-config = true
      - name: Setup cuenv
        run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || true
      - name: docs.deploy
        run: cuenv task docs.deploy -e production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
