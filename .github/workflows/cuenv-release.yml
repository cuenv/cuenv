# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: cuenv-release
on:
  release:
    types:
    - published
  workflow_dispatch:
    inputs:
      tag_name:
        description: Tag to release (e.g., v0.16.0)
        required: true
        type: string
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: write
  id-token: write
jobs:
  nix-build-darwin-arm64:
    name: nix.build (darwin-arm64)
    runs-on: namespace-profile-cuenv-macos-arm64
    timeout-minutes: 60
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: nix.build (darwin-arm64)
      run: cuenv task nix.build -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CUENV_ARCH: darwin-arm64
        RUSTC_WRAPPER: sccache
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nix-build-darwin-arm64
        path: result/bin/*
        if-no-files-found: ignore
        include-hidden-files: true
  nix-build-linux-x64:
    name: nix.build (linux-x64)
    runs-on: namespace-profile-cuenv-linux-x86
    timeout-minutes: 60
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: nix.build (linux-x64)
      run: cuenv task nix.build -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CUENV_ARCH: linux-x64
        RUSTC_WRAPPER: sccache
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nix-build-linux-x64
        path: result/bin/*
        if-no-files-found: ignore
        include-hidden-files: true
  publish-cue:
    name: publish.cue
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
    - nix-build-darwin-arm64
    - nix-build-linux-x64
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: Download nix-build-darwin-arm64
      uses: actions/download-artifact@v4
      with:
        name: nix-build-darwin-arm64
        path: dist/darwin-arm64
    - name: Download nix-build-linux-x64
      uses: actions/download-artifact@v4
      with:
        name: nix-build-linux-x64
        path: dist/linux-x64
    - name: publish.cue
      run: cuenv task publish.cue -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PATHS: dist/**/*
        TAG: ${{ github.ref_name }}
        RUSTC_WRAPPER: sccache
  publish-github:
    name: publish.github
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
    - nix-build-darwin-arm64
    - nix-build-linux-x64
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: Download nix-build-darwin-arm64
      uses: actions/download-artifact@v4
      with:
        name: nix-build-darwin-arm64
        path: dist/darwin-arm64
    - name: Download nix-build-linux-x64
      uses: actions/download-artifact@v4
      with:
        name: nix-build-linux-x64
        path: dist/linux-x64
    - name: publish.github
      run: cuenv task publish.github -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PATHS: dist/**/*
        TAG: ${{ github.ref_name }}
        RUSTC_WRAPPER: sccache
  docs-deploy:
    name: docs.deploy
    runs-on: blacksmith-8vcpu-ubuntu-2404
    needs:
    - docs-build
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: Download docs-build-artifacts
      uses: actions/download-artifact@v4
      with:
        name: docs-build-artifacts
        path: docs/dist
    - name: docs.deploy
      run: cuenv task docs.deploy -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        RUSTC_WRAPPER: sccache
  docs-build:
    name: docs.build
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: |-
        . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && \
        nix develop -c cargo build --release -p cuenv && \
        { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || \
          echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && \
        ./target/release/cuenv sync -A
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Setup sccache
      uses: mozilla-actions/sccache-action@v0.0.9
      env:
        RUSTC_WRAPPER: sccache
    - name: docs.build
      run: cuenv task docs.build -e production --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        RUSTC_WRAPPER: sccache
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: docs-build-artifacts
        path: docs/dist
        if-no-files-found: ignore
        include-hidden-files: true
