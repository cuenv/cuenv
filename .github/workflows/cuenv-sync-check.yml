# Generated by cuenv - do not edit manually
# Regenerate with: cuenv sync ci

name: cuenv-sync-check
on:
  push:
    branches:
    - main
    paths:
    - contrib
    - cue.mod
    - cue.mod/**
    - env.cue
    - schema
    - schema/**
  pull_request:
    branches:
    - main
    paths:
    - contrib
    - cue.mod
    - cue.mod/**
    - env.cue
    - schema
    - schema/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  ci-sync-check:
    name: ci.sync-check
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Build cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix develop -c cargo build --release -p cuenv && { echo "$(pwd)/target/release" >> "$GITHUB_PATH" 2>/dev/null || echo "$(pwd)/target/release" >> "$BUILDKITE_ENV_FILE" 2>/dev/null || true; } && cuenv sync -A
    - name: Setup Cachix (cuenv)
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix-env -iA cachix -f https://cachix.org/api/v1/install && cachix use cuenv
      env:
        CACHIX_CACHE_NAME: cuenv
        CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: Authenticate with crates.io
      uses: rust-lang/crates-io-auth-action@v1
    - name: Setup sccache
      uses: Mozilla-Actions/sccache-action@v0.2
      env:
        RUSTC_WRAPPER: sccache
    - name: ci.sync-check
      run: cuenv task ci.sync-check --skip-dependencies
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CACHIX_CACHE_NAME: cuenv
        CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
        RUSTC_WRAPPER: sccache
