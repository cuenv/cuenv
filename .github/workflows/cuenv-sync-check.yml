# Generated by cuenv - do not edit manually
# Regenerate with: cuenv ci --format github

name: cuenv-sync-check
on:
  push:
    branches:
    - main
    paths-ignore:
    - docs/**
    - examples/**
    - '*.md'
    - LICENSE
    - .vscode/**
  pull_request:
    branches:
    - main
    paths-ignore:
    - docs/**
    - examples/**
    - '*.md'
    - LICENSE
    - .vscode/**
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  checks: write
  pull-requests: write
jobs:
  ci-sync-check:
    name: ci.sync-check
    runs-on: blacksmith-8vcpu-ubuntu-2404
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@v16
      with:
        extra-conf: accept-flake-config = true
    - name: Setup Cachix
      uses: cachix/cachix-action@v15
      with:
        name: cuenv
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        pushFilter: (-source$|nixpkgs\.tar\.gz$)
    - name: Build cuenv
      run: . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh && nix build .#cuenv --accept-flake-config && { echo "$(pwd)/result/bin" >> $GITHUB_PATH 2>/dev/null || echo "$(pwd)/result/bin" >> $BUILDKITE_ENV_FILE 2>/dev/null || true; }
    - name: ci.sync-check
      run: cuenv task ci.sync-check
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
