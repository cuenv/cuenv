# Configuration for cargo-deny
# https://embarkstudios.github.io/cargo-deny/

[licenses]
# Confidence threshold for detecting a license from a license text.
confidence-threshold = 0.8
allow = [
    "MIT",
    "MIT-0",              # MIT No Attribution - used by borrow-or-share
    "Apache-2.0", 
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "AGPL-3.0-or-later",  # Our project license
    "Zlib",               # Used by foldhash dependency
    "Unicode-3.0",        # Used by unicode-ident
    "MPL-2.0",            # Mozilla Public License 2.0
    "CDLA-Permissive-2.0", # Community Data License Agreement - used by webpki-roots for root cert data
    "OpenSSL",            # OpenSSL License - used by aws-lc-sys for AWS TLS
    "CC0-1.0",            # Creative Commons Zero - used by notify for file watching
]
# Some crates don't have (easily) machine readable licensing information,
# adding a clarification or exception can help
exceptions = []

# Note: [bans] section removed to avoid overly strict dependency checking
# Focus on security advisories and license compliance instead

[advisories]
# The lint level for unmaintained crates  
unmaintained = "all"
# The lint level for crates that have been yanked from their source registry
yanked = "deny"
# A list of advisory IDs to ignore. 
ignore = [
    # rsa timing side-channel (Marvin Attack) - transitive dep from octocrab->jsonwebtoken
    # Used only for GitHub API JWT auth, not for cryptographic operations we control
    "RUSTSEC-2023-0071",
    # rustls-pemfile unmaintained - transitive dep from yup-oauth2
    # Functionality moved to rustls-pki-types, waiting for upstream update
    "RUSTSEC-2025-0134",
    # proc-macro-error unmaintained - transitive dep from clap/derive_more
    "RUSTSEC-2025-0057",
    # wasmtime advisories - transitive dep from extism 1.21.0
    # extism currently requires wasmtime/wasi-common/wiggle ^41; patched versions are outside this range.
    # A local lift to wasmtime 44 is not source-compatible with extism 1.21.0.
    # TODO: Remove when extism releases a version with patched wasmtime.
    "RUSTSEC-2026-0006",
    "RUSTSEC-2026-0020",
    "RUSTSEC-2026-0021",
    "RUSTSEC-2026-0085",
    "RUSTSEC-2026-0086",
    "RUSTSEC-2026-0087",
    "RUSTSEC-2026-0088",
    "RUSTSEC-2026-0089",
    "RUSTSEC-2026-0091",
    "RUSTSEC-2026-0092",
    "RUSTSEC-2026-0093",
    "RUSTSEC-2026-0094",
    "RUSTSEC-2026-0095",
    "RUSTSEC-2026-0096",
    "RUSTSEC-2026-0114",
    # rustls-webpki advisories - transitive dep from dagger-sdk 0.20.8
    # dagger-sdk is at the latest published version and still depends on reqwest 0.11/graphql_client 0.13.
    # TODO: Remove when dagger-sdk releases a version using reqwest 0.12+ / rustls 0.23+.
    "RUSTSEC-2026-0098",
    "RUSTSEC-2026-0099",
    "RUSTSEC-2026-0104",
]
[sources]
# Lint level for what to happen when a crate from a crate registry that is
# not in the allow list is encountered
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
