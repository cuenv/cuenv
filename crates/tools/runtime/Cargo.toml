[package]
name = "cuenv-tool-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Tool provider runtime for the cuenv ecosystem"

# No [lints] opt-in yet: this crate was extracted from cuenv-core, which
# predates the workspace lint config. The per-crate lint ratchet is
# RFC-0006 phase 6.

[dependencies]
cuenv-core = { workspace = true }
cuenv-manifest = { workspace = true }

async-trait = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
sha2 = { workspace = true }
dirs = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }
